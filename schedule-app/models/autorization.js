//var user = require('user');
var autorization = Object.create(user);
autorization = {

  //No properties
  
  // methods

  //Verifies if the user session is still valid
  isLogged: function () {
    //
  },

  //Secures the calls before they are proceeded to access the data
  authorization: function () {
    //
  },
 
}

module.exports = authorization;